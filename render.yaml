services:
  - type: web
    name: openclaw-zoom-agent
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: MODEL
        value: openai/gpt-4o
